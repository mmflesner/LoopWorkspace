git_url("https://github.com/mmflesner/Match-Secrets.git")

storage_mode("git")
type("appstore")

# Optional, but recommended
readonly(true)

# Pass app_identifier via Fastfile (as you're already doing) or here:
# app_identifier(["com.63QVLH6NM7.loopkit.Loop"])  # <-- hvis du vil hardkode

# You can also set branch if it's not master:
# git_branch("main")

# Docs for advanced config: https://docs.fastlane.tools/actions/match
